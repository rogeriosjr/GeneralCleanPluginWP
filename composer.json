{
    "name": "cleangeneral/general-clean",
    "description": "Protocolo de limpeza segura do WordPress",
    "type": "wordpress-plugin",
    "autoload": {
        "psr-4": {
            "CleanGeneral\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test-coverage": "mkdir -p reports && phpunit --coverage-clover reports/clover.xml"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "require": {}
}
